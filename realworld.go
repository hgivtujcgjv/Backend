package main

import (
	"net/http"
)

// сюда писать код
// по сути мы имеем три сущности , первая это article(obj) и его
// внутренняя сущность массив коментариев profile(obj)

func GetApp() http.Handler {
	return nil
}
